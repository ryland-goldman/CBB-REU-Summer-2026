## 1.8 Runge-Kutta

In this section we describe the method GPT uses to solve equation [1.23]. This method is known as fifth- order embedded Runge-Kutta with adaptive stepsize control. The algorithm is efficient and has the capability to specify the accuracy of the calculations. For a detailed description we refer to Numerical Recipes [2] but we will briefly explain the algorithm here.

Basically the algorithm consists of a fifth-order Runge-Kutta step. It advances y(𝑡) to y(𝑡+ ℎ) by performing the following steps: **k**1 = ℎ**f**(𝑡, **y**) **k**2 = ℎ**f**(𝑡+ 𝑎2ℎ, **y** + 𝑏21**k**1) ⋮ **k**6 = ℎ**f**(𝑡+ 𝑎6ℎ, **y** + 𝑏61**k**1 + ⋯+ 𝑏65**k**5) **y**(𝑡+ ℎ) =** y**(𝑡) + 𝑐1**k**1 + 𝑐2**k**2 + 𝑐3**k**3 + 𝑐4**k**4 + 𝑐5**k**5 + 𝑐6**k**6 + 𝒪(ℎ6) [1. 24] The values of the constants *a*, *b* and *c* can be found in Table 1-E. To be able to monitor the accuracy of y(𝑡+ ℎ) each step is calculated again using an embedded fourth-order formula: 𝒚∗(𝑡+ ℎ) =** y**(𝑡) + c1 ∗**k**1 + c2 ∗**k**2 + c3 ∗**k**3 + c4 ∗**k**4 + c5 ∗**k**5 + c6 ∗**k**6 + 𝒪(ℎ5) [1. 25] The error estimate ∆ is given by: ∆≡y(𝑡+ ℎ) −y∗(𝑡+ ℎ) = ∑(𝑐𝑖−𝑐𝑖 ∗) 𝑖=1 k𝑖 [1. 26] It is an indicator of the error made in the timestep. It is this difference that is kept to a specified degree of accuracy. Not too large, because this indicates an insufficient precision of the result. Not too small either, because a higher accuracy than needed is a waste of CPU time. Keeping the difference to the specified accuracy is done by adjusting the stepsize *h*.

∆ is a vector of all the coordinates of all the particles. The maximum absolute values for the position and momentum coordinates are defined as *x*max and 𝑚𝑐𝛾𝛽max respectively. They are compared with *x*err and 𝛾𝛽err, set in the in the inputfile to define the desired accuracy. The error indicator Λ is defined as the largest of the two following ratios: Λ = max (𝛾𝛽max 𝛾𝛽err , 𝑥max 𝑥err ) [1. 27] If Λ > 1, thus 𝛾𝛽max > 𝛾𝛽err or 𝑥max > 𝑥err, the step is not accepted and will be retried with: ℎnew = {𝑆 Λ 4 ℎold if 𝑆 Λ 4 > 0.10 0.10 ℎold otherwise [1. 28] where *h*new is chosen in such a way that for the next timestep Λ will approximately equal the safety parameter *S*. *S* is default set to 0.85. The timestep can decrease by no more than a factor of 10.

If Λ ≤1 the step is accepted with **y**(*t*+*h*). The next timestep is performed with: ℎnew = {𝑆 Λ 5 ℎold if 𝑆 Λ 5 < 5.0 5 ℎold otherwise [1. 29] This ensures that no more than a factor of 5 increase in the stepsize is possible.

Table 1-E: Cash-Karp constants for embedded Runge-Kutta method. *a**i** *

*b**ij** *

*c**i** * *c*****i** *

−

−

−

*j *=
