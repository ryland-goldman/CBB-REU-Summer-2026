## 4.4 Accelerating structures

This section contains the build-in accelerating structures with time-varying electromagnetic fields. Please note that only the `TE110gauss` and `TMx10gauss` elements have proper transverse fringe fields for the entrance and exit fields. If these models can not be applied, and if transverse fringes are required, please see the fieldmaps section 4.7.

4.4.1 TE011cylcavity
4.4.2 TE011gauss
4.4.3 TE110gauss
4.4.4 TErectcavity
4.4.5 TM010cylcavity
4.4.6 TM110gauss
4.4.7 TM110cylcavity
4.4.8 TM010gauss
4.4.9 TMrectcavity
4.4.10 Trwcell
4.4.11 Trwlinac
4.4.12 Trwlinbm

### 4.4.1 TE011cylcavity

```
TE011cylcavity(ECS,L,R,A,phi) ;
```
Cylindrically symmetric pillbox cavity in TE011 mode.

```
ECS

```
Element coordinate system.

```
L

```
Total cavity length in z direction [m].

```
R

```
Radius of the cavity [m].

```
A

```
Multiplication factor for all fields [T].

```
phi

```
Phase factor:  in radians.

The fields are calculated by [6]: ( ) ( ) ( )             +              = +              = +              = *t* *z* *r* *J* *R* *E* *t* *z* *r* *J* *R* *B* *t* *z* *r* *J* *B* *r* *z* sin cos sin sin sin cos

```
L
R
A
L
R
L
A
L
R
A

```
where –`L`<2 *z*<`L` and where *x*11≈3.8317 is the first zero of J1. The resonance frequency is given by:

```
R
L
```
*c* + =   where *c* is the speed of light. Please note that this element does not model fringe fields. To model a cavity with fringe fields, please use the `TE011gauss` element.

### 4.4.2 TE011gauss

```
TE110gauss(ECS,A,sigma,phi,omega) ;
```
Cylindrically symmetric rf cavity in TE011 mode with Gaussion on-axis *B*z field profile.

```
ECS
```
Element coordinate system.

```
A
```
Maximum amplitude [T]

```
sigma
```
Width of the Gaussian profile [m]

```
phi
```
Phase offset [rad]

```
omega
```
Angular frequency [rad/s]

This element models a standing wave rf cavity in TE011 mode. The on-axis longitudinal magnetic profile is assumed to be Gaussian with maximum amplitude `A` and width `sigma`:

) cos( exp ) (    +      − = *t* *z* *A* *z* *B**z*

The off-axis fields are derived from a 5th order expansion. Consequently, this element can only be used for particles relatively close to the *z*-axis.

### 4.4.3 TE110gauss

```
TE110gauss(ECS,A,sigma,phi,omega) ;
```
Cylindrically symmetric rf cavity in TE110 mode with Gaussion on-axis *E*x field profile.

```
ECS
```
Element coordinate system.

```
A
```
Maximum amplitude [V/m]

```
sigma
```
Width of the Gaussian profile [m]

```
phi
```
Phase offset [rad]

```
omega
```
Angular frequency [rad/s]

This element models a standing wave rf cavity in TE110 mode. The on-axis deflection profile is assumed to be Gaussian with maximum amplitude `A` and width `sigma`: ) cos( exp ) (    +      − = *t* *z* *A* *z* *E**x*

The off-axis fields are derived from a 5th order expansion. Consequently, this element can only be used for particles relatively close to the *z*-axis.

### 4.4.4 TErectcavity

```
TErectcavity(ECS,a,b,L,m,n,p,const,phi);
```
Rectangular TEmnp mode resonant cavity.

```
ECS

```
Element coordinate system.

```
a

```
Total cavity length in x-direction [m].

```
b

```
Total cavity length in y-direction [m].

```
L

```
Total cavity length in z-direction [m].

```
m

```
Mode index in x-direction.

```
n

```
Mode index in y-direction.

```
p

```
Mode index in z-direction.

```
Bo

```
Multiplication factor for all fields.

```
phi

```
Phase factor:  in radians.

The fields generated by this element are given by [6]: ( ) ( )                           +                                   −                   = +                                                       −                   − = *t* *d* *z* *p* *b* *y* *n* *a* *m* *a* *m* *d* *z* *p* *b* *y* *n* *a* *m* *b* *n* *k* *B* *t* *d* *z* *p* *b* *y* *n* *a* *m* *d* *z* *p* *b* *y* *n* *a* *m* *b* *n* *d* *p* *k* *d* *z* *p* *b* *y* *n* *a* *m* *a* *m* *d* *p* *k* *B* *c* *c* *c* cos sin cos sin sin sin cos sin sin cos cos cos sin cos cos cos sin **E** **B** where

Accelerating structures

*c* *d* *p* *b* *n* *a* *m* *k* *d* *p* *k* *b* *n* *a* *m* *k* *c* *c*             =               +       +       = = =         −       =       +       =

### 4.4.5 TM010cylcavity

```
TM010cylcavity(ECS,L,R,A,phi) ;
```
Cylindrically symmetric pillbox cavity in TM010 mode.

```
ECS

```
Element coordinate system.

```
L

```
Total cavity length in z direction [m].

```
R

```
Radius of the cavity.

```
A

```
Multiplication factor for all fields [V/m].

```
phi

```
Phase factor:  in radians.

The fields are calculated by [6]: ( ) ( )* **c* *t* *r* *J* *B* *t* *r* *J* *E**z* / cos sin      +        = +        =

```
R
A
R
A

```
where* c* is the speed of light, x01≈2.4048 is the first zero of J0 and the resonance frequency is *c* *p* *c* *k*

```
R
```
= = 

The resonant wavelength is given by:

```
R
```
.2 = =* **k*  

Please note that this element does not model fringe fields and consequently can only be used to study longitudinal dynamics. To model a cavity with fringe fields, please use `TM010gauss` or for example `map1D_TM` or `map25D_TM`.

### 4.4.6 TM110gauss

```
TM110gauss(ECS,A,sigma,phi,omega) ;
```
Cylindrically symmetric rf cavity in TM010 mode with Gaussion on-axis field profile.

```
ECS
```
Element coordinate system.

```
A
```
Maximum amplitude [T]

```
sigma
```
With of the Gaussian profile [m]

```
phi
```
Phase offset [rad]

```
omega
```
Angular frequency [rad/s]

This element models a standing wave rf cavity in TM110 mode. The on-axis acceleration profile is assumed to be Gaussian with maximum amplitude `A` and width `sigma`: ) cos( exp ) (    +      − = *t* *z* *A* *z* *B**y*

The off-axis fields are derived from a 5th order expansion. Consequently, this element can only be used for particles relatively close to the *z*-axis.

### 4.4.7 TM110cylcavity

```
TM110cylcavity(ECS,L,R,A,phi) ;
```
Cylindrically symmetric pillbox cavity in TM110 mode.

```
ECS
```
Element Coordinate System.

```
L
```
Total cavity length in z direction [m].

```
R
```
Outer radius of the cavity.

```
A
```
Multiplcations factor for all fields.

```
phi
```
Phase factor in radians.

The electromagnetic fields of this element are in cylindrical coordinates given by: ) sin( ) cos( / / ) cos( ) sin( ) sin( ) cos(

```
phi
R
R
R
phi
R
phi
R
```
+       −       − = +       − = +       = *t* *r* *r* *J* *r* *J* *B* *t* *r* *r* *J* *B* *t* *r* *J* *E* *r* *z*          where *x*110≈3.8317 is the first zero of J1 and ω110=*c·x*110/`R` with *c* the speed of light. To avoid the singularities at *r*=0, the following Taylor expansion of the fields is used for all *r*<10–6: ) cos( )8 ' ( ) ' ( ' ) cos( ' ' ) sin( '

```
phi
phi
phi
```
+ − − − − = + − = + = *t* *c* *y* *y* *B* *t* *c* *y* *B* *t* *E* *y* *z*    where *x*’=*x*110 *x*/`R` and *y*’=*x*110 *y*/`R` are the dimensionless position variables.

Example: Idealized cylindrically symmetric 1 mT, 5 cm, 3 Ghz TM110 cavity at *z*=10 cm. 1.

```
freq=2998e6 ;
# European 3 GHz
```
2.

```
x110=3.8317 ;
# First zero of J
```
3.

```
R   =c*x110 / (2*pi*freq) ;
# Calculate Radius based on frequency
```
4.

```
Bmax=1e-3 ; # Maximum field: 1 mT
```
5.

```
TM110cylcavity("wcs", "z",0.1, 0.05,R,2*c*Bmax,phi) ;

### 4.4.8 TM010gauss

```
```
TM010gauss(ECS,A,sigma,phi,omega) ;
```
Standing wave cylindrically symmetric TM010 cavity with Gaussian acceleration profile

```
ECS
```
Element coordinate system

```
A
```
Maximum amplitude [V/m]

```
sigma
```
With of the Gaussian profile [m]

```
phi
```
Phase offset [rad]

```
omega
```
Angular frequency [rad/s]

This element models a standing wave rf cavity in TM010 mode. The on-axis acceleration profile is assumed to be Gaussian with maximum amplitude `A` and width `sigma`: ) cos( exp ) (    +      − = *t* *z* *A* *z* *E**z*

The off-axis fields are derived from a 5th order expansion. Consequently, this element can only be used for particles relatively close to the *z*-axis.

A particle entering this cavity, such that its *z*=0 position corresponds to *t*=0, encounters an integrated field Δ*V* given by: ) cos( exp ) cos( exp              − = +      − =     − *A* *dz* *z* *z* *A* *V*

Accelerating structures where it is assumed that the cavity does not introduce significant velocity differences. Under that condition, a given energy gain/loss ΔV corresponds to an *A* to use for gaussTM010 given by: ) cos( exp           −  = *V* *A*

where φ specifies the phase relative to maximum acceleration.

### 4.4.9 TMrectcavity

```
TMrectcavity(ECS,a,b,L,m,n,p,const,phi);
```
Rectangular TMmnp mode resonant cavity.

```
ECS

```
Element coordinate system.

```
a

```
Total cavity length in x-direction [m].

```
b

```
Total cavity length in y-direction [m].

```
L

```
Total cavity length in z-direction [m].

```
m

```
Mode index in x-direction.

```
n

```
Mode index in y-direction.

```
p

```
Mode index in z-direction.

```
Eo

```
Multiplication factor for all fields.

```
phi

```
Phase factor:  in radians.

The fields generated by this element are given by [6]: ( ) ( )                           +                                   −                   = +                                                       −                   − = *t* *d* *z* *p* *b* *y* *n* *a* *m* *a* *m* *d* *z* *p* *b* *y* *n* *a* *m* *b* *n* *c* *k* *E* *t* *d* *z* *p* *b* *y* *n* *a* *m* *d* *z* *p* *b* *y* *n* *a* *m* *b* *n* *d* *p* *k* *d* *z* *p* *b* *y* *n* *a* *m* *a* *m* *d* *p* *k* *E* *c* *c* *c* cos cos sin cos cos cos sin sin cos sin sin sin cos sin sin sin cos **B** **E** where *c* *d* *p* *b* *n* *a* *m* *k* *d* *p* *k* *b* *n* *a* *m* *k* *c* *c*             =               +       +       = = =         −       =       +       =

### 4.4.10 Trwcell

```
trwcell(ECS,Ezef,phi,w,L) ;
```
Resonant cavity of a  traveling wave buncher or linac.

```
ECS
```
Element Coordinate System.

```
Ezef
```
Effective **E** field [V/m].

```
phi
```
Phase factor:  in radians.

```
w
```
Angular frequency: .in [s-1].

```
L
```
Cell length [m].

The fields generated by this element are given in cylindrical coordinates by:

*c* *E* *B* *k* *k* *r* *k* *z* *k* *t* *M* *E* *r* *k* *z* *k* *t* *M* *E* *r* *t* *z* *t* *z* *r* *t* *z* *z* = − + = − + =      ) ( I) cos( ) ( I) sin( O where the following definitions are used: Ezef L    = − = = *M* *c* *k* *k* *k* *z* *t* *z*

The factor in front of `Ezef` is the average of a sine in range   − . It converts peak field to average field and is introduced for compatibility.

The formulas are proposed by G. Saxon [7].

### 4.4.11 Trwlinac

```
trwlinac(ECS,ao,Rs,Po,P,Go,thetao,phi,w,L) ;
```
Constant gradient  traveling wave buncher or linac without beamloading.

```
ECS
```
Element Coordinate System.

```
ao
```
Initial attenuation constant: 0.

```
Rs
```
Shunt impedance: Rs.

```
Po
```
Input power, design value: P0 [W].

```
P
```
Input power, actual power used: P [W].

```
Go
```
Design gamma at entrance: 0.

```
thetao
```
Bunch phase with respect to wave, design value: 0.

```
phi
```
RF phase offset: .

```
w
```
Angular frequency: .in [s-1].

```
L
```
Length of the structure: L [m].

An rf–wave with constant amplitude travels through the structure. The phase velocity of the wave increases along the structure in such a way that an electron entering the linac with initial energy 0mc2 is accelerated but remains in the same position relative to the wave. For this to take place, the linac needs to be operated at its design power Po and the particle must be input at the design phase 0 with respect to the ‘crest’ of the wave. It is possible however to operate the linac at a different power than the design power and the particles can enter at any phase.

According to the design values the amplitude and phase of the field that accelerates the particles is: ' )' ( ) cos(      + − = =  *z* *z* *s* *dz* *z* *k* *t* *P* *R* *E* where z is the longitudinal position of the particle relative to the beginning of the structure.

To calculate the integral, we first calculate the particle velocity using the linear increase in energy from 0mc2 to 0mc2+E0qez: ( ) ) ( *Fz* *z* + − =   where F is the normalized design acceleration: *c* *m* *q* *E* *F* *e* *e* − =

The longitudinal and transverse wavenumbers are given by: ) ( *z* *k* *k**z*  = , *k* *k* *k* *z* *t* − = where *c* *k*  =

Accelerating structures

Yielding:      − − − + = *z* *z* *Fz* *F* *k* *dz* *z* *k* ) ( ' )' (  

The amplitude and phase of the actual accelerating field are due to the rf input power P and the rf phase . They are given by:     + − = =  *z* *z* *s* *dz* *z* *k* *t* *P* *R* *E* ' )' (

P may differ from Po and  does not need to be chosen such that the particles enter the linac at phase 0.

The fields produced in cylindrical coordinates are: *c* *E* *B* *k* *k* *r* *k* *I* *E* *E* *r* *k* *I* *E* *E* *r* *t* *z* *t* *r* *t* *z* = = =    ) ( ) cos( ) ( ) sin(

This element is shaped using an erf function at the entrance and exit of the structure. The characteristic length is 1 mm. The shaping does not affect the numerical results but improves the simulation speed considerably because it reduces the sudden turn on and off of the fields.

### 4.4.12 Trwlinbm

```
trwlinbm(ECS,ao,Rs,Po,P,Ib,Go,thetao,phi,w,L) ;
```
Constant gradient  traveling wave buncher or linac with selfconsistent beamloading.

```
ECS
```
Element Coordinate System.

```
ao
```
Initial attenuation constant: 0.

```
Rs
```
Shunt impedance: Rs.

```
Po
```
Input power, design value: P0 [W].

```
P
```
Input power, actual power used: P [W].

```
Ib
```
Beam current: Ib.

```
Go
```
Design gamma at entrance: 0.

```
thetao
```
Bunch phase with respect to wave, design value: 0.

```
phi
```
RF phase offset: .

```
w
```
Angular frequency: .in [s-1].

```
L
```
Length of the structure: L [m].

The electromagnetic fields generated by this element are separated into two independent waves: • The accelerating rf-wave. • The beam induced beamloading wave.

The accelerating wave is calculated in precisely the same way as in trwlinac, described in section 4.4.11. The beamloading wave has the same structure, i.e. the same wavenumbers, but different amplitude and phase. When the linac is run at its design values, the beamloading field opposes the accelerating field and the field’s amplitude increases along the structure. When the linac is used far off its nominal settings, analytical expressions or correct approximations for the beamloading wave can not be derived. To allow this model to be used for all input settings, differential equations for the beamloading amplitude and phase are solved while tracing the particles.

The accelerating rf-wave has amplitude *E*1 and  as defined in section 4.4.11. The beamloading wave has amplitude *E*b and phase +b. Because the structure of both waves is similar, the total fields are:

( ) ( ) *c* *E* *B* *k* *k* *r* *k* *I* *E* *E* *E* *r* *k* *I* *E* *E* *E* *r* *t* *z* *t* *b* *b* *r* *t* *b* *b* *z* = + + = + + =        ) ( ) cos( ) cos( ) ( ) sin( ) sin(

Both the amplitude and phase of the beamloading wave are calculated from the particle trajectories. However, an amplitude and phase representation is not very efficient computationally. Therefore the beamloading wave is internally characterized by the amplitudes *u* and *v* of two independent waves, 90 out of phase.    − → + + → +     = = ) sin( ) cos( ) cos( ) cos( ) sin( ) sin( ) sin( ) cos(           *u* *A* *u* *A* *A* *A* *u* *b* *b* *b* *b*

The differential equations for *u* and *v* to be solved are then given by: ) cos( ) sin(       *N* *z* *R* *I* *dt* *dz* *dz* *dv* *dt* *dv* *N* *z* *R* *I* *dt* *dz* *dz* *du* *dt* *du* *z* *s* *b* *z* *s* *b* − = = − = = with boundary condition = = = = *t* *t* *u*

The *u* and *v* representation is transformed back using: ) arctan( *u* *u* *E* *b* *b* = + = 

The amplitude and the phase of the beamloading wave are written in the outputfile as `Eb` and `phib`.

Static electric fields
