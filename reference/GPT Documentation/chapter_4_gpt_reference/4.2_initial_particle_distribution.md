## 4.2 Initial particle distribution

The following elements can be used to specify the initial particle distribution. More information can be found in the corresponding tutorial sections 2.9 and 2.10.

4.2.1 Set manipulation
4.2.1.1 SetParticles
4.2.1.2 SetFile
4.2.1.3 SetScale
4.2.1.4 SetTransform
4.2.1.5 SetExtrapolate
4.2.1.6 SetMove
4.2.1.7 SetCopy
4.2.1.8 SetCurvature
4.2.1.9 SetTCopy
4.2.1.10 SetShuffle
4.2.1.11 SetReduce
4.2.1.12 SetRmacrodist
4.2.1.13 SetStartPar
4.2.1.14 SetStartLine
4.2.1.15 SetStartxyzGrid
4.2.2 X, Y and Z manipulators
4.2.2.1 AddZoscillation
4.2.2.2 SetXdist
4.2.2.3 SetXYdistbmp
4.2.2.4 SetYdist
4.2.2.5 SetZdist
4.2.2.6 SetRxydist (2D)
4.2.2.7 SetPhidist
4.2.2.8 SetEllipse
4.2.3 GBx, GBy and GBz manipulators
4.2.3.1 AddGoscillation
4.2.3.2 SetGBxdist
4.2.3.3 SetGBydist
4.2.3.4 SetGBzdist
4.2.3.5 SetGBrxydist (2D)
4.2.3.6 SetGBphidist
4.2.3.7 SetGBthetadist (2D spherical)
4.2.3.8 SetGdist
4.2.3.9 SetGBxemittance
4.2.3.10 SetGByemittance
4.2.4 X–GBx, Y–GBy and Z–G manipulators
4.2.4.1 AddGzoscillation
4.2.4.2 AddXDiv
4.2.4.3 AddYDiv
4.2.4.4 AddZDiv
4.2.5 Time-dependent particle distributions
4.2.5.1 SetTdist

### 4.2.1 Set manipulation

The following keywords perform an action on an entire particle set.

##### 4.2.1.1 SetParticles

```
setparticles(set,N,m,q,Qtot);
```
Create a particle set with a specified number of particles.

- **`set`** — Name of particle set to create.

- **`N`** — Number of particles in this set.

- **`m`** — Mass of elementary particle in this set [kg].

- **`q`** — Charge of elementary particle in this set [C].

- **`Qtot`** — Total charge of all particles in this set [C].

The total charge `Qtot` is uniformly distributed among the particles: The number of elementary particles every macro-particle represents is given by:

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
= *n*

One of the space-charge elements as described in section 4.8 must be used to include space-charge calculations. Otherwise, the `Qtot` parameter is ignored.

##### 4.2.1.2 SetFile

```
setfile(set,filename) ;
```
Read a complete particle distribution from a GDF file.

- **`set`** — Name of particle set to generate.

- **`filename`** — Name of the GDF file containing the particle coordinates.

This element reads a macro-particle distribution from `filename` into the specified particle `set`. All particles are set relative to ‘wcs’. The file can be created using `asci2gdf` or an outputfile of a previous GPT run can be specified. The GDF File must contain at least the following arrays: Particle *x*-coordinates [m]

- **`y`** — Particle *y*-coordinates [m]

- **`z`** — Particle *z*-coordinates [m]

- **`GBx`** — Particle normalized *x*-momentum: px/mc.

- **`GBy`** — Particle normalized *y*-momentum: py/mc.

- **`GBz`** — Particle normalized *z*-momentum: pz/mc.

Instead of the momentum coordinates, it is also possible to specify the velocity components:

- **`Bx`** — Particle normalized *x*-velocity: vx/c.

- **`By`** — Particle normalized *y*-velocity: vy/c.

- **`Bz`** — Particle normalized *z*-velocity: vz/c.

Optionally, the following arrays can be present:

- **`t`** — Release time of the particle [s]. Use this column to set the initial temporal distribution. If this column is not present, all particles start at *t*=0.

- **`G`** — Particle Lorentz factor. If not specified, the Lorentz factor is calculated from: ( ) /1

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
+ + − = . Explicitly specifying `G` slightly improves the numerical accuracy of the calculations, but this effect is only noticeable when `G `> 106.

- **`m`** — Mass of the elementary particles this macro-particle represents. Default: See below.

- **`q`** — Charge of the elementary particles this macro-particle represents. Default: See below.

- **`nmacro`** — Number of elementary particles this macro-particle represents. Default: 0.

- **`rmacro`** — Typical radius of this macro-particle. See the space-charge elements for a detailed description of this parameter. Default: 0.

The mass and charge of the elementary particles can be specified as separate arrays. When these are not present, the user-defined variables **m** and **q** are read from the inputfile. When also these are not specified, the mass and charge of an electron are used. To start protons, without specifying their mass and charge in `m` and `q` columns, the following lines could be used:

```
m =  mp ;
q = -qe ;
setfile("protons","file.gdf") ;

If the file is a GPT outputfile created with a `tout(t);` statement, this group must be specified by a `time=t;` statement above `setfile`. Outputfiles created by MR can also be used. Either use MR again with exactly the same parameter sweep or specify `var=value` above `setfile` to specify which group has to be used. Listing 4-3 and Listing 4-2 show an example how to start particles from a previous GPT run.

```
**Listing 4-1:** `file1.in`: First inputfile writing output at t=1 ns to `file1.gdf`

```
setparticles("beam",1000,10*me,10*qe,Qtot) ;
setrxydist("beam","u",1,2) ;
setphidist("beam","u",0,2*pi) ;
...
tout(1e-9) ;
```
Listing 4-2: `file2.in`: Second inputfile, reading the particle coordinates from `file1.gdf`

```
time=1e-9 ;
setfile( "beam", "file1.gdf" ) ;
...
tout(2e-9) ;
```

##### 4.2.1.3 SetScale

```
setscale(set,x,y,z,GBx,GBy,GBz,[t,[nmacro,rmacro]] ;
```
Multiplies all initial particle coordinates by a specified constant

- **`set`** — Particle set Scale factor for the *x*-coordinates

- **`y`** — Scale factor for the *y*-coordinates

- **`z`** — Scale factor for the *z*-coordinates

- **`GBx`** — Scale factor for the *γβ**x *-coordinates

- **`GBy`** — Scale factor for the *γβ**y* -coordinates

- **`GBz`** — Scale factor for the *γβ**z* -coordinates

- **`t`** — Optional scale factor for the release times

- **`nmacro`** — Optional scale factor for the number of elementary particles

- **`rmacro`** — Optional scale factor for the macro particle radius

This element multiplies all initial particle coordinates in a set by a specified constant. It is particularly useful to scale the initial coordinates read from an external file with `setfile`.

Example: If, for example, the file `particles.gdf` has *x*, *y* and *z*-coordinates in [mm], they can be converted to [m] as follows: 1.

```
mm=1e-3 ;
```
2.

```
setfile("beam","particles.gdf") ;
```
3.

```
setscale("beam",mm,mm,mm,1,1,1) ;
```

##### 4.2.1.4 SetTransform

```
settransform(ecs,set);
```
Apply a coordinate transform on the particles in a set.

- **`ECS`** — Transform specifiction.

- **`set`** — Name of particle set.

The particle coordinates are modified according to the specified `ECS`. When the `ECS` specification transform does not start with `"wcs"`, all particles are moved to the specified `CCS`. In that case only local and global elements specified relative to the same `CCS` will affect the particle trajectories.

Example: `settransform("wcs","z",2, "set") ; ` –2 –2 z [m] z [m]

##### 4.2.1.5 SetExtrapolate

```
setextrapolate(set,time) ;
```
Linearly extrapolate the initial particle distribution to a given time

- **`set`** — Name of the particle set

- **`time`** — Desired time [s]

This element modifies the position coordinate of all particles in the specified `set` by linearly extrapolating them to the specified `time`. The equations applied are:

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
= − + = *t* *t*** ****v** **r** **r** ) ( old new where **v** is the velocity of the particle. This element does not affect the momentum coordinates.

##### 4.2.1.6 SetMove

```
setmove(setfrom,setto);
```
Move all particles in a set to a different set.

- **`setfrom`** — Original particle set.

- **`setto`** — Set to add particles to.

All particles in `setfrom` will be moved to the particles in `setto`.

##### 4.2.1.7 SetCopy

```
setcopy(set,N,dz) ;
```
Multi-Copy a particle set in the z-direction

- **`set`** — Particle set.

- **`N`** — Number of copies (including original).

- **`dz`** — Distance between copies in z-direction [m], may be negative.

The `setcopy` element makes `N` duplicates of all particles in a specific set in the z-direction. The spacing between the different bunches is `dz`, as shown below.

- **`dz`** — `N `copies

To copy a particle set in time domain, please use `settcopy`.

##### 4.2.1.8 SetCurvature

```
setcurvature(set, Ra, Rc, Lc) ;
```
Model photo-emission from a curved cathode.

- **`set`** — Particle set.

- **`Ra`** — Aperture [m].

- **`Rc`** — Cathode curvature radius [m]. Specify zero for a flat surface.

- **`Lc`** — Laser curvature radius [m]. Specify zero for a flat laser front.

This element changes the position and release time coordinates of all particles in the specified set to model photo-emission from a curved cathode.

As illustrated in Figure 4-24, a hollow cathode is simulated with a negative value for `Rc`. This moves all initial particle coordinates in the negative z-direction; the ones nearest to the axis the most. Combined with a flat laser front, a hollow cathode releases particles furthest away from the axis first.

A curved laser front is simulated with a non-zero value for `Lc`. The sign of `Lc` is chosen identical to the sign of `Rc`. Combined with a flat cathode surface, `Rc`=0, a negative value for `Lc` releases particles near the axis first. When `Lc` is equal to `Rc` (with identical sign), the release time of the particles is not affected.

`Rc`<0

```
Ra
Z=0 `Rc`=0 `Rc`>0

```
```
Ra
Z=0 Z=0

```
Figure 4-1: Spherical cathode for negative, zero and positive `Rc`. All particles *i* with *r*i>`Ra` or *r*i>`Lc` are removed and a warning is printed.

The initial position coordinates are changed as function of particle radius *r*i, cathode aperture `Ra` and cathode curvature `Rc`: ( ) ( )       − − − + =  − − − − = 

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
*if* *r* *if* *if* *r* *z*

The release time coordinates are changed as function of particle radius *r*i, the initial position offset *z*i and the laser curvature `Lc`. ( ) ( )        − − − − − =  −   − − − − − =  / / / / / *Lc* *if* *c* *z* *c* *La* *r* *Lc* *if* *c* *z* *Lc* *if* *c* *z* *c* *La* *r* *t*

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
where *La* is the minimum of `Ra` and |`Lc`|.

##### 4.2.1.9 SetTCopy

```
settcopy(set,N,dt) ;
```
Multi-Copy a particle set in time domain.

- **`set`** — Particle set.

- **`N`** — Number of copies (including original).

- **`dt`** — Distance between copies [s], may be negative.

The `settcopy` element makes `N` duplicates of all particles in a set, equidistantly spaced in time domain. The spacing between the different bunches is `dt`, as shown below.

- **`dt`** — `N `copies` `

`settcopy` is typically used to start a number of identical bunches as function of time. The example below starts five identical 1 ps bunches with 5 ps separation. Divergence was added to better show the effect of the element.

Figure: Five identical 1 ps bunches, started one after the other with, 5 ps separation. The plot shows a snapshot at *t*=23 ps.

```
# Bunch Parameters
G   =    10  ; # Lorentz factor Gamma=10
len =  1e-12 ; # Bunch length 1 [ps]
R   =  1e-3  ; # Initial bunch radius 1 [mm]
div =  1e+3  ; # Divergence of 100 [rad/m]

# Multi-bunch parameters
dt  =  5e-12 ; # Bunch spacing 5 [ns] ;
Nb  =  5     ; # 5 Bunches total
# Create initial cylindrical bunch
setparticles("beam",250,me,qe,0.0) ;
settdist("beam","u",0,len) ;
setrxydist("beam","u",R/2,R) ;
setphidist("beam","u",0,2*pi) ;
setGdist("beam","u",G,0) ;
addxdiv("beam",0,div) ;
addydiv("beam",0,div) ;

# Copy particle set
settcopy("beam",Nb,dt) ;

# Output from 0 to 30 [ps]
tout(0,30e-12,1e-12) ;

```

##### 4.2.1.10 SetShuffle

```
setshuffle(set);
```
Randomize the order of all particles in the set.

The location of the particles in the specified `set` is randomized. Because this will not affect the actual particle coordinates, none of the distributions is affected.

##### 4.2.1.11 SetReduce

```
setreduce(set,N);
```
Reduce the number of particles in a set.

- **`set`** — Selected particle set

- **`N`** — Final number of particles in the set.

This element randomly removes all but` N` particles in the specified set. As shown in Figure 4-2, this operation affects the smoothness and accuracy of the original distribution considerably. Therefore it should never be used without a verification that the resulting particle set is as desired. time=2.3e-011 z [mm] -2 -1 x [mm]

Figure 4-2: Uniform distribution containing 1000 particles (top left), reduced to 500, 200 and 100 particles.

##### 4.2.1.12 SetRmacrodist

```
setrmacrodist(set,distribution);
```
Sets the distribution for the particle radii.

The radius of all particles in the `set` is modified according to the specified `distribution`. The distribution is interpreted as a 1D distribution.

This element can be used to specify the radius of the particles in a set. This radius is used by `spacecharge3D`, `spacecharge2Dline` and related elements to assign a volume to the individual macro- particles. `spacecharge2Dcircle` ignores `setrmacrodist` due to the complexity of the equations.

Example: The following line sets the radius of all particles in set `"beam"` to `R`:

```
setrmacrodist("beam","u",R,0) ;
```

##### 4.2.1.13 SetStartPar

```
setstartpar(set,x,y,z,GBx,GBy,GBz,[m,q,n])
```
Add one macro-particle to a particle set.

- **`set`** — Name of particle set. Particle *x*-coordinate [m].

- **`y`** — Particle *y*-coordinate [m].

- **`z`** — Particle *z*-coordinate [m].

- **`GBx`** — Particle normalized *x*-momentum: vx/c.

- **`GBy`** — Particle normalized *y*-momentum: vy/c.

- **`GBz`** — Particle normalized *z*-momentum: vz/c.

- **`m`** — Mass [kg] of the elementary particles this macro-particle represents, typically `me`.

- **`q`** — Charge [C] of the elementary particles this macro-particle represents, typically `qe`.

- **`n`** — Number of elementary particles this macro-particle represents.

Use this function to start one particle with a specified position and momentum. The mass and charge parameters are optional. When not specified, the inputfile variables `m` and `q` are used. An empty particle set is created before the particle is added if the specified `set` does not exist.

##### 4.2.1.14 SetStartLine

```
setstartline(set,N,x1,y1,z1,x2,y2,z2,[GBx,Gby,GBz,[m,q,n]])
```
Start a number of particles on a line.

- **`set`** — Name of particle set. time=0, N=1000 - 0.5 0.0 0.5 z Count time=0, N=500 - 0.5 0.0 0.5 z Count time=0, N=200 - 0.5 0.0 0.5 z Count time=0, N=100 - 0.5 0.0 0.5 z Count

- **`N`** — Number of particles to generate, minimum 2.

- **`x1`** — Start *x*-position of line [m].

- **`y1`** — Start *y*-position of line [m].

- **`z1`** — Start *z*-position of line [m].

- **`x2`** — End *x*-position of line [m].

- **`y2`** — End *y*-position of line [m].

- **`z2`** — End *z*-position of line [m].

- **`GBx`** — Particle normalized *x*-momentum: vx/c.

- **`GBy`** — Particle normalized *y*-momentum: vy/c.

- **`GBz`** — Particle normalized *z*-momentum: vz/c.

- **`m`** — Mass [kg] of the elementary particles this macro-particle represents, typically `me`.

- **`q`** — Charge [C] of the elementary particles this macro-particle represents, typically `qe`.

- **`n`** — Number of elementary particles this macro-particle represents.

Use this function to start `N` particles equidistantly spaced on a straight line, optionally with a specified momentum. The endpoints are always included.

The mass and charge parameters are optional, but can only be specified when the momentum is given. When not specified, the inputfile variables `m` and `q` are used. An empty particle set is created before the particle is added if the specified `set` does not exist.

Example: Start 101 particles between 0 and 10 m on the z-axis.

```
setstartline("beam",101, 0,0,0, 0,0,10 ) ;
```

##### 4.2.1.15 SetStartxyzGrid

```
setstartxyzgrid(set,Lx,dx,Ly,dy,Lz,dz,[GBx,GBy,GBz]) ;
```
Starts the macro-particles in a rectangular grid with equal velocity.

- **`set`** — Name of particle set.

- **`Lx`** — Length of array in **x-**direction. This length may be zero.` `

- **`dx`** — Spacing in **x**-direction.` `

- **`Ly`** — Length of array in **y**-direction. This length may be zero.` `

- **`dy`** — Spacing in **y**-direction.` `

- **`Lz`** — Length of array in **z**-direction. This length may be zero.` `

- **`dz`** — Spacing in **z**-direction.` `

- **`GBx`** — Particle normalized *x*-momentum: vx/c.

- **`GBy`** — Particle normalized *y*-momentum: vy/c.

- **`GBz`** — Particle normalized *z*-momentum: vz/c.

The particles start in a rectangular grid centered around the origin. The grid extends from –`Lx`/2,–`Ly`/2, – `Lz`/2 to `Lx`/2,`Ly`/2,`Lz`/2 with spacing `dx`,`dy`,`dz`.

It is simple to instruct GPT to initialize billions of particles using this keyword. Since GPT has no built-in limit for the number of particles, it must rely on the operating system to indicate an ‘out of memory’ condition. On some virtual-memory machines, this error message may come too late, causing either GPT or the complete system to crash.

### 4.2.2 X, Y and Z manipulators

The following keywords modify the *x*-, *y*- and *z*-coordinates of the particles. The distribution syntax is listed in section 1.6.1.

##### 4.2.2.1 AddZoscillation

```
addzoscillation(set,alpha,kz) ;
```
Add a periodic longitudinal density modulation

- **`set`** — Name of the particle set

- **`alpha`** — Amplitude of the oscillation []

- **`kz`** — Wavenumber [m−1]

This element modifies the longitudinal coordinate of all particles in the specified `set` according the following equation: ) sin( old old new *z* *z* *z*

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
 + =

Example: Add a modulation in *z*.

1.

```
setparticles("beam",1e6,me,qe,0) ;
```
2.

```
setzdist("beam","u",0,1e-3) ;
```
3.

```
setGdist("beam","u",100,1) ;
```
4.

```
addzoscillation("beam",0.2,20000) ;
```
5.

```
tout(0) ;

```

The amplitude of an oscillation is often expressed in terms of the bunching factor b(k) defined as:  = *z* *k* *N* *k* *b* ) cos( ) ( After running `addzoscillatoin`, the new distribution has a bunching factor of: ( ) ( ) ) ( ) ( sin ) cos( if ) sin( )) sin( sin( )) sin( cos( ) cos( ) sin( cos cos ) ( old old i, old i, old i, old i, old i, old i, old i, old i, new i,       +  −   −  + = =     *k* *b* *z* *k* *z* *k* *N* *z* *k* *z* *k* *z* *k* *z* *k* *N* *z* *k* *z* *k* *N* *z* *k* *N* *k* *b*

##### 4.2.2.2 SetXdist

```
setxdist(set,distribution);
```
Set *x* coordinate distribution.

Modifies *x* [m] such that

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
=

The *x-*coordinate of all particles in the `set` is modified according to the specified `distribution`. The distribution is interpreted as a 1D distribution. To specify a uniform distribution between –1 and 1 centered around zero, the following line can be used:

```
setxdist("beam","u",0,2) ;
```

##### 4.2.2.3 SetXYdistbmp

```
setxydistbmp(set,filename,[xres,yres]) ;
```
Sets the xy-projection of the initial particle distribution based on a grayscale bitmap image

- **`set`** — Particle set.

- **`filename`** — Name of the grayscale bitmap file.

- **`xres`** — Horizontal resolution [pixels/meter].

- **`yres`** — Vertical resolution [pixels/meter].

This element sets the xy-projection of the initial particle distribution according to the specified grayscale bitmap image. Each pixel of the image is treated as a rectangular surface area with a uniformly distributed chance of emitting particles, given by the pixel color. Black pixels have zero chance of emitting particles, -
0.4 -
0.2
0.0
0.2
0.4 GPT z [mm]
9
9.5
1
0
0.0
1
0
0.5 Gamma white pixels have maximum chance and grayscale values are linearly interpolated in between. This element is very convenient for the simulation of photoemission in for example an rf-photogun, where an actual image of the laser focus on the cathode plane can be used to set the initial particle distribution.

The **filename** must be a grayscale, 8-bit per pixel, uncompressed MS-Windows bitmap file, typically having extension bmp or dib. Reverse row order is supported, but RLE compression is not. A variety of other image formats such as TIFF can easily be converted to grayscale MS-Windows bitmaps with image processing software such as Adobe® Photoshop®.

The pixel size, as stored in the bitmap file, can be overruled with the `xres` and `yres` parameters. The bitmap is placed with its center in the origin. The horizontal and vertical axes of the image are aligned with the *x*- and *y*-axis respectively. To move the center and/or change the alignment, please use the `settransform` element after `setxydistbmp`.

**Example: ** We used Adobe® Photoshop® to create a 1000x1000 test bitmap of an elliptical hard-edge bunch with checkerboard modulation, as shown in the left plot of Figure 4-3: The following GPT inputfile sets the xy- projection of the initial particle distribution with 100k electrons based on this bitmap. 4.

```
setparticles("beam",100000,me,qe,0) ;
```
5.

```
setxydistbmp("beam","test.bmp") ;
```
6.

```
tout(0) ;

```
Figure 4-3: Left: Original test.bmp grayscale bitmap of an elliptical bunch with checkerboard modulation. Right: Corresponding GPTwin plot of the initial particle distribution with 100k particles. Because white pixels emit most particles while particles are shown in black in the right plot, the plots look like color-reversed versions of each other.

**Technical notes: ** Grayscale bitmap files are essentially indexed-color images with 256 or less grayscale color entries. `setxydistbmp` does not use the 8-bit color-index to determine the particle density, but actually looks up the corresponding RGB pixel color from the color table. This ensures correct results even in case the color table is not filled with 256 monotonically increasing grayscale colors. An error message is produced if the red, green and blue values of any pixel in the image are not identical.

The newer V4 and V5 BITMAPINFOHEADER structures are recognized, but the added functionality is ignored because this only concerns color information.

##### 4.2.2.4 SetYdist

```
setydist(set,distribution);
```
Set *y*-coordinate distribution.

Modifies *y *[m] such that *i**y*

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
=

The *y*-coordinate of all particles in the `set` is modified according to the specified `distribution`. The distribution is interpreted as a 1D distribution.

##### 4.2.2.5 SetZdist

```
setzdist(set,distribution);
```
Set longitudinal particle distribution.

Modifies *z* [m] such that *i**z*

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
=

The *z*-coordinate of all particles in the `set` is modified according to the specified `distribution`. The distribution is interpreted as a 1D distribution.

##### 4.2.2.6 SetRxydist (2D)

```
setrxydist(set,distribution);
```
Set radial distribution in *x*-coordinate.

Modifies *x* [m] such that *D* |

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
=

The *x*-coordinate of all particles in the `set` is modified according to the specified `distribution`. The distribution is interpreted as a 2D distribution. This keyword is typically followed by:

```
setphidist(set,"u",0,2*pi) ;
```

##### 4.2.2.7 SetPhidist

```
setphidist(set,distribution);
```
Set phi distribution.

Modifies: *x *and, *y *such that: ) sin( ) cos( *rxy* *y* *rxy* *y* *rxy*

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
= = + =

The angle in the xy–plane of all particles in the `set` is modified according to the specified distribution. The distribution is interpreted as a 1D distribution. The *z*-distribution of the particles will not be affected.

##### 4.2.2.8 SetEllipse

```
setellipse(set,a,b,c);
```
Set the xyz distribution as a uniformly filled ellipsoid.

- **`set`** — Name of particle set.

- **`a`** — Size in x-dimension [m].` `

- **`b`** — Size in y-dimension [m].` `

- **`c`** — Size in z-dimension [m].` `

The ellipsoid is aligned with the primary axes where **a**, **b** and **c** specify the maximum *x*, *y* and *z* coordinate respectively.

### 4.2.3 GBx, GBy and GBz manipulators

The following keywords modify the *x*-, *y*- and *z*-coordinates of the particles. In many cases, the Lorentz factor  is modified simultaneously. The distribution syntax is listed in section 1.6.1.

##### 4.2.3.1 AddGoscillation

```
addGoscillation(set,alpha,kz) ;
```
Add a periodic density modulation in energy

- **`set`** — Name of the particle set

- **`alpha`** — Amplitude of the oscillation []

- **`kG`** — Wavenumber []

This element modifies the longitudinal momentum coordinates of all particles in the specified `set` according the following equation: ) sin( ) ( new old new old − − − = + = + = *y* *z* *old*            

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
Example: Add a modulation in energy

1.

```
setparticles("beam",1e6,me,qe,0) ;
```
2.

```
setzdist("beam","u",0,1e-3) ;
```
3.

```
setGdist("beam","u",100,1) ;
```
4.

```
addGoscillation_fix("beam",0.2,20) ;
```
5.

```
tout(0) ;

setGBxdist(set,distribution);
```
Sets the momentum distribution in *x*-direction.

Modifies: *x* such that

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
= 

The x-coordinates of the particles in the `set` are modified according to the specified distribution. The distribution is interpreted as a 1D distribution.

##### 4.2.3.3 SetGBydist

```
setGBydist(set,distribution);
```
Sets the momentum distribution in *y*-direction.

Modifies *y *such that *i**y*

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
= 

The y-coordinates of the particles in the `set` are modified according to the specified distribution. The distribution is interpreted as a 1D distribution.

##### 4.2.3.4 SetGBzdist

```
setGBzdist(set,distribution);
```
Sets the momentum distribution in *z*-direction.

Modifies *z *such that *i**z*

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
= 

The *z*-coordinates of the particles in the `set` are modified according to the specified distribution. The distribution is interpreted as a 1D distribution.

##### 4.2.3.5 SetGBrxydist (2D)

```
setGBrxydist(set,distribution);
```
Sets radial distribution in *x*-coordinate.

Modifies *x *such that *D* |

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
= 

-
0.4 -
0.2
0.0
0.2
0.4 GPT z [mm]
9
9.5
1
0
0.0
1
0
0.5 Gamma

The *x*-coordinate of all particles in the `set` is modified according to the specified `distribution`. Although the distribution is interpreted as a 2D distribution, the *x* coordinate is left unchanged. This keyword is therefore typically followed by:

```
setGBphidist(set,"u",0,2*pi) ;
```

##### 4.2.3.6 SetGBphidist

```
setGBphidist(set,distribution);
```
Set phi distribution.

Modifies *x* and *y* such that: ) sin( ) cos( *rxy* *y* *rxy* *y* *rxy*

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
         = = + =

The angle in the *x**y*–plane of all particles in the `set` is modified according to the specified distribution. The distribution is interpreted as a 1D distribution. The theta distribution of the particles will not be affected.

##### 4.2.3.7 SetGBthetadist (2D spherical)

```
setGBthetadist(set,distribution);
```
Set the -θ distribution.

Modifies x y and z such that: ) cos( / ) sin( / ) sin( *rxyz* *z* *rxy* *y* *rxyz* *y* *rxy* *rxyz* *z* *y* *rxyz* *y* *rxy*

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
                      = = = + + = + =

The x-, y- and z-coordinates of the particles in the `set` are modified to create the specified -θ distribution. The distribution is interpreted as a 2D spherical distribution. The - and -φ distribution of the particles will not be affected. When x and y are both zero, a phi angle of zero is assumed.

##### 4.2.3.8 SetGdist

```
setGdist(set,distribution);
```
Set the  distribution.

Modifies z such that: − − − = = *y* *z*       

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
The *z*-coordinates of the particles in the `set` are modified to create the specified -distribution. The distribution is interpreted as a 1D distribution.

Useful equations: *e**q* *mc* *eV* *E* / )1 ( ] [ energy

Kinetic factor

Lorentz − = − =   

##### 4.2.3.9 SetGBxemittance

```
setGBxemittance(set,emittance);
```
Sets the emittance in xx-space. Modifies: x.

- **`set`** — Selected particle set

- **`emittance`** — Requested emittance in xx-space in [m-rad]

This element sets the emittance in xx-space by scaling all x-coordinates linearly using: ( ) ' ' ' *c* *c* *c* *c* *c* *c* −  = − = − =

- **`emittance`** —     

Because this element modifies the x-coordinates, it should not be used after `setGdist`. A warning is printed when over one order of magnitude or more needs to be scaled.

This element sets the emittance in transverse position and momentum space. Therefore, the results can be slightly different from the results calculated by the `nemixrms` routines, especially in the presence of a large energy spread.

The element assumes all particles in the set to represent an equal amount of elementary particles.

##### 4.2.3.10 SetGByemittance

```
setGByemittance(set,emittance);
```
Sets the emittance in yy-space. Modifies: y.

- **`set`** — Selected particle set

- **`emittance`** — Requested emittance in yy-space in [m-rad]

This element sets the emittance in yy-space by scaling all y-coordinates linearly using: ( ) ' ' ' *c* *c* *c* *c* *y* *y* *c* *c* *y* *y* *y* *y* *y* *y* *y* *y* *y* *y* −  = − = − =

- **`emittance`** —     

Because this element modifies the y-coordinates, it should not be used after `setGdist`. A warning is printed when over one order of magnitude or more needs to be scaled.

This element sets the emittance in transverse position and momentum space. Therefore, the results can be slightly different from the results calculated by the `nemiyrms` routines, especially in the presence of a large energy spread.

The element assumes all particles in the set to represent an equal amount of elementary particles.

### 4.2.4 X–GBx, Y–GBy and Z–G manipulators

The following keywords skew the phase-space areas. They will not affect the transverse or longitudinal emittance, nor will they affect the *x*-, *y*- and *z*-distributions. The distribution syntax is listed in section 1.6.1.

##### 4.2.4.1 AddGzoscillation

```
addGzoscillation(set,alpha,kz) ;
```
Add a periodic energy modulation correlated with the z-coordinate

- **`set`** — Name of the particle set

- **`alpha`** — Amplitude of the oscillation []

- **`kz`** — Wavenumber [m−1]

This element modifies the longitudinal momentum coordinates of all particles in the specified `set` by adding an oscillatory energy modulation with respect to the *z*-coordinate. The equations applied are: ) sin( ) ( new old new old − − − = + = + = *y* *z* *z*           

- **`kz`** — Example: Add a modulation in energy correlated with *z*.

1.

```
setparticles("beam",1e6,me,qe,0) ;
```
2.

```
setzdist("beam","u",0,1e-3) ;
```
3.

```
setGdist("beam","u",100,1) ;
```
4.

```
addGzoscillation("beam",0.2,20000)
```
5.

```
tout(0) ;

```

##### 4.2.4.2 AddXDiv

```
addxdiv(set,xc,div);
```
Add divergence in the x-x plane.

- **`set`** — Selected particle set.

- **`xc`** — Center of *x*-distribution [m], typically 0.

- **`div`** — Divergence, see equations below.

The x-coordinate is modified by: ) ( − − − = −  + = *y* *z*        

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
A linear x-x-dependence is added, keeping the transverse emittance constant. To keep the particle energy and the longitudinal emittance constant, the *z*-coordinte is modified accordingly.

Please note that although a bunch is typically characterized by a radius *R* and a divergence angle , this element does not have  as its divergence parameter. The reason for that is that it would give unintuitive results for hard-edges beams.

##### 4.2.4.3 AddYDiv

```
addydiv(set,yc,div);
```
Add divergence in the y-y-plane

- **`set`** — Selected particle set.

- **`yc`** — Center of *y*-distribution [m], typically 0.

- **`div`** — Divergence, see equations below.

The *y*-coordinate is modified by: ) ( − − − = −  + = *y* *z* *y* *y* *y*        

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
A linear y-y dependence is added, keeping the transverse emittance constant. To keep the particle energy and the longitudinal emittance constant, the GBz-coordinte is modified accordingly.

##### 4.2.4.4 AddZDiv

```
addzdiv(set,zc, div1,[div2,[div3,[...]]]);
```
Add divergence in the z--plane.

- **`set`** — Selected particle set.

- **`zc`** — Center of *z*-distribution [m].

- **`div1`** — Divergence in [eV/m].

- **`div2`** — Second order term in [eV/m2]. - 0.4 - 0.2 0.0 0.2 0.4 GPT z [mm] 9 9.5 1 0 0.0 1 0 0.5 Gamma

- **`div3`** — Third order term in [eV/m3].

The z-coordinate is modified by: ( ) ) ( ) ( ) ( − − − = + −  + −  + −   + = *y* *new* *z* *e* *old* *new* *z* *z* *z* *mc* *q*         

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
A z--dependance is added to the initial particle distribution. When only a linear term is added, the longitudinal emittance remains constant. This element has no effect on the transverse emittance or transverse particle distribution.

### 4.2.5 Time-dependent particle distributions

Default, all particles are emitted at once at *t*=0 in the simulation. This can be changed using the elements presented in this section to start particles at any time in the simulation.

##### 4.2.5.1 SetTdist

```
settdist(set,distribution);
```
Set particle-release time distribution.

Modifies `t` [s] such that *i**t*

*[equation/table lost in PDF extraction — see UserManual.pdf §4.2]*
=

The release time of the particles in the `set` is modified according to the specified `distribution`. The distribution is interpreted as a 1D distribution. To specify a uniform distribution between –1 ns and 1 ns centered around zero, the following line can be used:

```
settdist("beam","u",0,2e-9) ;

```
Timestep and output control
