## 3.16 GDFSOLVE

```
gdfsolve [-hHv] [–o outfile] infile.sol program [parameters]
```
Multidimensional solver for the specified program

```
-o outfile
```
Write output to a file named `outfile`

```
-a outfile
```
Append output to a file named `outfile`

```
-h
```
Give short help and quit.

```
-H
```
Give long help and quit.

```
-v
```
Verbose mode. Display iteration output containing all variables and constraints.

```
infile.sol
```
Name of the file containing information about the parameters that are to be varied and the constraints to be met. See Table 3-I for specific sections in this file.

```
program [parameters]
```
Command line of a program that creates a GDF file and recognizes the `-o` option, for example GPT.

GDFsolve can be used to polish off a solution, based on a good initial estimate. The primary method used is based on multidimensional Newton-Raphson, providing quadratic convergence in the vicinity of a solution. GDFsolve can also be used as multidimensional optimizer, but this feature is less useful as it is far less efficient and sensitive to local minima and simulation noise. Table 3-I: Sections in the `.sol `file and a GDFsolve output description Section Section Description

```
[OPTIONS]
```
3.16.1 Sets various options and constants directly related to the algorithm.

```
[VARIABLES]
```
3.16.2 Defines the variables to be varied.

```
[CONSTRAINTS]
```
3.16.3 Defines the constraints to be met.

```
[OPTIMIZE]
```
3.16.4 Defines the constraints to be minimized or maximized.

3.16.5 Output description of GDFsolve

Information about the internal algorithms of GDFsolve can be found in sections 1.12.1 to 1.12.2. A sample `.sol` file without an optimize section is shown in Listing 3-2.

**Listing 3-2:** Sample .sol for GDFsolve: Find Isol resulting in an avgr of 1 mm. 22. `# Solve average beam radius (avgr) = 1 mm by varying Isol. ` 23. ` ` 24. `[OPTIONS] ` 25. `backtracking = 0 ; ` 26. ` ` 27. `[VARIABLES] ` 28. `Isol = 1000 ; `

```
# Define variable to vary, starting value is 1000
Isol.reldelta = 0.01 ;

# Use 1% difference to obtain derivative information
Isol.min = 0 ;

# Isol must be positive
```

[CONSTRAINTS]
avgr = 1.0e-3 ;

```
# avgr must be 1 mm
avgr.pos = 1 ;

# at z=1 m
avgr.abstol = 0.1e-3 ;

# with a tolerance of +/- 0.1 mm

### 3.16.1 Options

```
In the [OPTIONS] sections, the following options can be set. The syntax is shown in Table 3-J. Table 3-J: Options syntax. Syntax Required Default Description

```
[OPTIONS]
```
No

Start of options section.

```
forcejacobian = 1 ;
```
No Depends Forces a full Jacobian matrix to be calculated after each successful step. When not specified, Broydens’ update method is used when the number of variables is less or equal than the number of constraints, see section 1.12.1.5.

```
SVDtolerance = tol ;
```
No 0.001 Ratio between the largest and smallest Singular Value. When the ratio is smaller than `tol`, the corresponding direction is dropped.

```
minconvergence = a ;
```
No 0.01 Every step must bring the scaled constraints (1- `a`) closer to the solution. When this criterion can not be met, GDFsolve terminates with an error.

```
backtracking = 0 ;
```
No Disable the backtracking algorithm and terminate with an error when |**F**| increases.

```
keepgoing = 1 ;
No Disable all termination criteria and keep running until the executable is forced to stop externally. Using this option is not recommended for inexperienced users. `optimizetolerance = tol ; `No 0.05 The optimizer stops iterating when the target scaled rms value does not change more then `tol`.

```
```
ignoreerrors = 1 ;
```
No Assume all error codes from GPT and GDFA to be a very bad simulation result. Please use with care.

### 3.16.2 Variables

The [VARIABLES] section defines all variables to be varied. The syntax is shown in Table 3-K. Table 3-K: Variable syntax. Syntax Required Description

```
[VARIABLES]
```
Yes Start of variables section.

```
var = x ;
```
Yes Defines a new variable named `var`, with initial estimate `x.`

```
var.absdelta = abs ;
```
At least The absolute scaling of variable `var` is `abs`.

```
var.reldelta = rel ;
```
one The relative scaling of variable `var` is `rel`.

The total scaling is defined as: 𝑑𝑥= ±√`abs`2 + 𝑥2 ∙`rel`2 .

```
var.min = lb ;
```
No Variable lower bound for variable `var` is `lb`.

```
var.max = ub ;
```
No Variable upper bound for variable `var` is `ub`.

The scaling `delta` is important for the internal algorithms of GDFsolve because the derivative of all constraints is approximated by taking steps larger or equal to `delta`. As a result, a too small `delta` is very sensitive to simulation noise caused by a too low accuracy setting or insufficient particles in the simulation. On the other hand, a too large `delta` could send the derivative approximation into the non-linear regime. Before GDFsolve is used, we recommended first making a scan over a number of `delta`’s and plotting every constraint.

### 3.16.3 Constraints

The [CONSTRAINTS] section defines all constraints to be met simultaneously. The syntax is shown in Table 3-L.

Table 3-L: Constraints syntax Syntax Required Description

```
[CONSTRAINTS]
```
No Start of constraints section.

```
con = ft ;
```
Yes Define a new constraint named `con` to have target value `ft`.

```
con.time = t ;
```
One of Specify the time `t` when constraint `con` must be met.

```
con.position = p ;
```
these Specify the position `p` where constraint `con` must be met.

```
con.abstol = abs ;
```
At least The absolute tolerance of constraint `con` is `abs`.

```
con.reltol = rel ;
```
one The relative tolerance of constraint `con` is `rel`.

The total tolerance is defined as: 𝑑𝑓= √`abs`2 + 𝑓𝑡2 ∙`rel`2 .

The geometric length of all scaled constraints |**F**| is given by: |**F**| = √∑( `con` −`ft` 𝑑𝑓 ) [3. 14] A solution is found when |**F**| ≤1. Apart from the termination criterion, the tolerance is also used in the internal algorithms of GDFsolve as the inverse weight of the constraints.

When an identical constraint must be used at different times or locations, the `con` name can be prepended with `group:` notation. For example, the following section makes the average beam radius 3 mm at z=1 m and 2 mm at z=2 m. The tolerance is set to 0.1 mm in both cases.

```
[CONSTRAINTS]
a:avgr = 3.0e-3 ;
a:avgr.position = 1.0 ;
a:avgr.abstol = 0.1e-3 ; b:avgr = 2.0e-3 ;
b:avgr.position = 2.0 ;
b:avgr.abstol = 0.1e-3 ;

### 3.16.4 Optimize

```
The [OPTIMIZE] section defines the function(s) to be minimized. The syntax is shown in Table 3-M. Table 3-M: Optimize syntax. Syntax Required Description

```
[OPTIMIZE]
```
No Start of optimize section.

```
opt = ft ;
```
Yes Define a new function named `opt` with approximate minimum *ft*.

```
opt.time = t ;
```
One of Specify the time `t` when function `opt` must be minimized.

```
opt.position = p ;
```
these Specify the position `p` where constraint `opt` must be minimized.

```
opt.abstol = abs ;
```
At least The absolute tolerance of function `opt` is `abs``.`

```
opt.reltol = rel ;
```
one The relative tolerance of function `opt` is `rel``.`

The total tolerance is defined as: 𝑑𝑓= ±√`abs`2 + 𝑓𝑡2 ∙`rel`2 .

```
opt.weight = m ;
```
No Defines the weight of the constraint. When not specified 1 is assumed. A negative value must be specified to maximize a function.

When more than one function and/or additional constraints are specified, GDFsolve tries to minimize: 𝑓= |**F**| + ∑`m`

```
opt
```
𝑑𝑓 [3. 15] A negative weight parameter can be used to maximize a function.

### 3.16.5 Output

GDFSolve writes output when all constraints are met and/or when a minimum has been found. Without a solution, there is no output. The outputfile itself contains the settings of all variables and the values of the corresponding constraints, followed by the complete output of the successful GPT run. All variables and constraints can be inspected using GDF2A.

The output of GDFsolve can also be analyzed with GDFA like the output of a normal GPT run. Furthermore, the MR utility can be used in combination with GDFsolve. This solves for identical constraints as function

of a scanned parameter. This construction builds an outputfile of all successful GPT runs as function of the scanned parameter. GDFA can be used to analyze these results.

When the `–v` option is specified, GDFsolve writes iteration output to the messages window. It consists of the following columns:

```
N
```
The iteration number.

```
varN
```
Value of the N’th variable.

```
conN
```
Value of the N’th constraint to be solved. This value is followed by the scaled F value, the number of tolerances off the target, between parentheses. When for example `con1` has a value of 10, a target of 4 and a delta of 1, the display will be 10(6) indicating that the constraint is 6 tolerances off its desired value.

```
optN
```
Value of the N’th function to minimize. This value is followed by the scaled result, the number of tolerances off the estimated minimum times the weight, between parentheses. When for example opt1 has a value of 10, an estimated minimum of 4 and a delta of 1, the display will be 10(6) indicating that the value is expected to drop 6 tolerances.

```
rms
```
When GDFsolve is used as root finder, the rms value is the vector length of F, the scaled distance from a solution. When GDFsolve is used as optimizer, the scaled results of the functions to minimize are added to this value.

Only the differences in variables and constraints are displayed when the root finder is calculating the Jacobian as indicated by a J following the iteration number.
